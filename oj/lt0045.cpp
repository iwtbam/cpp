#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int jump(vector<int>& nums){

    }
};

int main()
{
    size_t size = 5;
    // vector<vector<int>> vv = {size, {size, 0}};

    // cout << vv.size() << endl;
    // cout << vv[0].size() << endl;

    return 0;
}